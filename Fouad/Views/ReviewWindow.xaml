<Window x:Class="Fouad.Views.ReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fouad.Views"
        mc:Ignorable="d"
        Title="{DynamicResource ReviewWindowTitle}" Height="800" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <TextBlock Text="{DynamicResource ReviewTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding FileName}" FontSize="14" Foreground="Gray"/>
        </StackPanel>
        
        <!-- Tab Control for Different Record Types -->
        <TabControl Grid.Row="1" Margin="10">
            <!-- New Records Tab -->
            <TabItem Header="{DynamicResource NewRecordsTab}">
                <DataGrid ItemsSource="{Binding NewRecords}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Decision}" Binding="{Binding SelectedDecision}" Width="100"/>
                        <DataGridTemplateColumn Header="{DynamicResource Actions}" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{DynamicResource Ignore}" 
                                                Command="{Binding IgnoreCommand}" 
                                                Width="60" Height="25" Margin="2"/>
                                        <Button Content="{DynamicResource AddAsNew}" 
                                                Command="{Binding AddAsNewCommand}" 
                                                Width="80" Height="25" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <!-- Dynamic columns will be added programmatically -->
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <!-- Match Records Tab -->
            <TabItem Header="{DynamicResource MatchRecordsTab}">
                <DataGrid ItemsSource="{Binding MatchRecords}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Decision}" Binding="{Binding SelectedDecision}" Width="100"/>
                        <DataGridTemplateColumn Header="{DynamicResource Actions}" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{DynamicResource Ignore}" 
                                                Command="{Binding IgnoreCommand}" 
                                                Width="60" Height="25" Margin="2"/>
                                        <Button Content="{DynamicResource Update}" 
                                                Command="{Binding UpdateCommand}" 
                                                Width="80" Height="25" Margin="2"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <!-- Existing and New data columns will be added programmatically -->
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            
            <!-- Disagreement Records Tab -->
            <TabItem Header="{DynamicResource DisagreementRecordsTab}">
                <DataGrid ItemsSource="{Binding DisagreementRecords}" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="{DynamicResource Decision}" Binding="{Binding SelectedDecision}" Width="100"/>
                        <DataGridTemplateColumn Header="{DynamicResource Actions}" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{DynamicResource Ignore}" 
                                                Command="{Binding IgnoreCommand}" 
                                                Width="50" Height="25" Margin="1"/>
                                        <Button Content="{DynamicResource Update}" 
                                                Command="{Binding UpdateCommand}" 
                                                Width="60" Height="25" Margin="1"/>
                                        <Button Content="{DynamicResource AddAsNew}" 
                                                Command="{Binding AddAsNewCommand}" 
                                                Width="60" Height="25" Margin="1"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <!-- Existing and New data columns will be added programmatically -->
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        
        <!-- Footer with Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="{DynamicResource ProcessDecisions}" 
                    Command="{Binding ProcessDecisionsCommand}" 
                    Width="120" Height="30" Margin="5"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="{DynamicResource Cancel}" 
                    Command="{Binding CancelCommand}" 
                    Width="80" Height="30" Margin="5"
                    Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>